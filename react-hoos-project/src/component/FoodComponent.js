function FoodComponent() {

    /*
        Hooks
        1. 변수
            지역변수 : let, const
            props : 속성값으로 받는 값 => 불변 (정적 상태값)
            state : 컴포넌트가 가질 수 있는 동적 상태값 (이벤트시에 값을 변경) /
     */
}

export default FoodComponent;
